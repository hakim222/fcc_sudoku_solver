const puzzleString = require('../controllers/puzzle-strings.js');
const chai = require('chai');
const assert = chai.assert;

const Solver = require('../controllers/sudoku-solver.js');
let solver = new Solver;

suite('Unit Tests', () => {
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
    test('Logic handles a valid puzzle string of 81 characters', function () {
        assert.isNotNull(solver.validate(puzzleString.puzzlesAndSolutions[0][0]), 'This is an optional error description - e.g. null is null');
    });
});
